import { createStore } from "vuex";

export default createStore({
    state() {
        return {
            // data 
        }
    },
    mutations: {
        // mutations를 통해서만 데이터 변경 가능
    },
    actions: {
        // method(함수)
    },
    getters: {
        // computed(계산)
    }
});